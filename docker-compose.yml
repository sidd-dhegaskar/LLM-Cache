services:
  redis:
    image: redis/redis-stack:latest
    ports:
      - "6379:6379"   # Redis protocol — app connects here
      - "8001:8001"   # RedisInsight web UI — browse keys/index at http://localhost:8001
    volumes:
      - redis_data:/data

volumes:
  redis_data:
